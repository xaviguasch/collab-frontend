export const userLogged = data => ({
  type: 'USER_LOGGED',
  data
});

export const getWallets = data => ({
  type: 'GET_WALLETS',
  data
});

export const getTransactions = data => ({
  type:'GET_TRANSACTIONS',
  data
});
